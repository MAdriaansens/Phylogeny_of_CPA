These scripts are auxiliary and are used in some way later on.


What they do is:


  create a .json file (which some find easier than a .tsv or fasta file) from the .tsv files

  
  Using a fasta file retrieve the full length using the protein id header. 
