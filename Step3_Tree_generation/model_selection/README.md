Model finder was ran on all the alignments from random sequences. 
The BIC and AIC scores plus df were then grepped into a seperate file and then used as input for a .rmd file.
From this we observed that there is a preference for a given set of models, LG+ R6-7 + CAT + Gamma
